# Local Postgres (or Neon POSTGRES_URL in production)
DATABASE_URL=postgresql://pguser@localhost:5433/orcaedge
# Anthropic (optional locally; without it the scorer runs in transparent heuristic mode)
ANTHROPIC_API_KEY=
# Resend (optional; without it nurture messages are generated + logged but not emailed)
RESEND_API_KEY=
